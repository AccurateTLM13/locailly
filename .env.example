LOCAL_AI_HOST=127.0.0.1
LOCAL_AI_PORT=31313
OLLAMA_BASE_URL=http://127.0.0.1:11434
# Ollama (optional overrides)
OLLAMA_MODEL=llama3.2

# Console validation CLI (optional; --model flag takes precedence)
# VALIDATION_MODEL=llama3.2

# Console validation artifacts: bundle | split | both (default: bundle)
# Use both during migration, then bundle for normal runs.
VALIDATION_ARTIFACT_MODE=bundle
PAGESPEED_API_KEY=
MEMORY_VALIDATION_VAULT_PATH=
